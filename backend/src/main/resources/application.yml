spring:
  application:
    name: IS-lab1
  datasource:
    url: jdbc:postgresql://localhost:5432/is_lab1
    username: tronget
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

